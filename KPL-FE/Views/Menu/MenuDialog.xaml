<Window x:Class="KPL_FE.Views.MenuDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        Title="Tambah Menu"
        Width="480" Height="700"
        MinWidth="400" MinHeight="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanMinimize"
        ShowInTaskbar="False"
        ui:WindowHelper.UseModernWindowStyle="True">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Hidden">
            <StackPanel Margin="0,0,8,0">
                <TextBlock x:Name="TitleText" Text="Tambah Menu" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,16" />

                <StackPanel>
                    <TextBlock Text="Nama" Margin="0,0,0,4" />
                    <TextBox x:Name="NameBox" MaxLength="100" />
                </StackPanel>

                <StackPanel Margin="0,12,0,0">
                    <TextBlock Text="Deskripsi" Margin="0,0,0,4" />
                    <TextBox x:Name="DescBox" Height="72" AcceptsReturn="True" TextWrapping="Wrap" MaxLength="255" />
                </StackPanel>

                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Harga (Rp)" Margin="0,0,0,4" />
                        <TextBox x:Name="PriceBox" MaxLength="12" />
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Kategori" Margin="0,0,0,4" />
                        <ComboBox x:Name="CategoryCombo" SelectedIndex="0">
                            <ComboBoxItem>Makanan</ComboBoxItem>
                            <ComboBoxItem>Minuman</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <CheckBox x:Name="AvailableCheck" Content="Tersedia" Margin="0,12,0,0" IsChecked="True" />

                <StackPanel Margin="0,12,0,0">
                    <TextBlock Text="URL Gambar" Margin="0,0,0,4" />
                    <TextBox x:Name="ImageUrlBox" MaxLength="2048"
                             ui:ControlHelper.PlaceholderText="https://contoh.com/gambar.jpg" />
                </StackPanel>

                <Border Margin="0,16,0,0" Height="244" CornerRadius="8" BorderThickness="1" 
                        BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}" 
                        Background="{DynamicResource SystemControlBackgroundAltHighBrush}" 
                        HorizontalAlignment="Stretch" ClipToBounds="True">
                    <Grid>
                        <!-- Placeholder when loading fails or URL is empty -->
                        <Grid x:Name="PlaceholderGrid" Visibility="Visible">
                            <ui:FontIcon Glyph="&#xEB9F;" FontSize="36" Opacity="0.4" VerticalAlignment="Center" HorizontalAlignment="Center" />
                            <TextBlock Text="No Image Preview" FontSize="11" Margin="0,0,0,24"
                                       HorizontalAlignment="Center" VerticalAlignment="Bottom" 
                                       Opacity="0.4" />
                        </Grid>
                        <!-- Live Image Preview -->
                        <Image x:Name="ImagePreview" Stretch="UniformToFill" 
                               RenderOptions.BitmapScalingMode="HighQuality"
                               ImageFailed="ImagePreview_ImageFailed" 
                               Visibility="Collapsed" />
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <StackPanel x:Name="SavingPanel" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0" Visibility="Collapsed">
                <ui:ProgressRing Width="20" Height="20" IsActive="False" />
                <TextBlock Text="Menyimpan..." Margin="8,0,0,0" VerticalAlignment="Center" Opacity="0.75" />
            </StackPanel>
            <Button x:Name="CancelButton" Click="CancelButton_Click" MinWidth="100" Margin="0,0,8,0">
                Batal
            </Button>
            <Button x:Name="SaveButton"
                    Click="SaveButton_Click" MinWidth="100"
                    IsEnabled="False">
                Simpan
            </Button>
        </StackPanel>
    </Grid>
</Window>
